// tailwind.config.js — En Tailwind v4 la configuración se hace en el CSS (@theme).
// Este archivo se mantiene vacío para compatibilidad con herramientas externas.
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
}
